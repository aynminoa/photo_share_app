<!DOCTYPE html>
<html>
  <head>
    <title>PhotoShareApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body class="w-50 mx-auto">
    <nav class="navbar navbar-expand-lg navbar-light bg-primary">
      <div class="container-fluid">
        <div class="pt-3">
          <div class="d-flex flex-row">
            <% if logged_in? %>
            <p class="mr-3"><%= link_to "Profile", user_path(current_user.id), class:"text-white" %></p>
            <P><%= link_to "Logout", session_path(current_user.id), class:"text-white", method: :delete %></P>
            <% else %>
            <p class="mr-3"><%= link_to "Sign up", new_user_path, class:"text-white" %></p>
            <p><%= link_to "Login", new_session_path, class:"text-white" %></p>
            <% end %>
          </div>
        </div>
      </nav>
      <% flash.each do |key, value| %>
      <%= content_tag(:div, value, class: "#{key}") %>
      <% end %>
    
    <%= yield %>
  
  </body>
</html>
